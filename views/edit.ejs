<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <title>Sửa Đặt Chỗ</title>
</head>
<body>
  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-md-6">
        <div class="card shadow-lg">
          <div class="card-header text-center bg-warning text-white">
            <h3>Sửa Đặt Chỗ</h3>
          </div>
          <div class="card-body">
            <form action="/bookings/<%= booking._id %>?_method=PUT" method="POST">
              <div class="mb-3">
                <label for="customerName" class="form-label">Tên khách hàng</label>
                <input 
                  type="text" 
                  id="customerName" 
                  name="customerName" 
                  class="form-control" 
                  value="<%= booking.customerName %>" 
                  required
                >
              </div>
              <div class="mb-3">
                <label for="date" class="form-label">Ngày đặt</label>
                <input 
                  type="date" 
                  id="date" 
                  name="date" 
                  class="form-control" 
                  value="<%= booking.date.toISOString().split('T')[0] %>" 
                  required
                >
              </div>
              <div class="mb-3">
                <label for="time" class="form-label">Giờ đặt</label>
                <input 
                  type="time" 
                  id="time" 
                  name="time" 
                  class="form-control" 
                  value="<%= booking.time %>" 
                  required
                >
              </div>
              <div class="mb-3">
                <label for="status" class="form-label">Trạng thái</label>
                <select id="status" name="status" class="form-select">
                  <option value="Pending" <% if (booking.status === 'Pending') { %>selected<% } %>>Pending</option>
                  <option value="Confirmed" <% if (booking.status === 'Confirmed') { %>selected<% } %>>Confirmed</option>
                  <option value="Cancelled" <% if (booking.status === 'Cancelled') { %>selected<% } %>>Cancelled</option>
                </select>
              </div>
              <div class="d-grid">
                <button type="submit" class="btn btn-success">Cập nhật đặt chỗ</button>
              </div>
            </form>
          </div>
          <div class="card-footer text-center">
            <a href="/bookings" class="btn btn-outline-secondary">Trở lại danh sách</a>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <!-- <script>
    // Hiển thị thông báo thành công
    Swal.fire({
      title: 'Thành công!',
      text: 'Đặt chỗ đã được thêm thành công.',
      icon: 'success',
      confirmButtonText: 'OK'
    });
  
    // Hiển thị thông báo lỗi
    Swal.fire({
      title: 'Lỗi!',
      text: 'Đặt chỗ đã tồn tại vào thời gian này.',
      icon: 'error',
      confirmButtonText: 'OK'
    });
  </script> -->
</body>
</html>
